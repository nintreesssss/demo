# 开始

这个仓库会包含自学JAVA过程中编写的一些程序.

## 文件夹结构

The workspace contains two folders by default, where:

- `src`: 这个文件夹存放源代码.
- `lib`: 这个文件夹维护依赖性.

已编译的输出文件会在 `bin` 文件夹里.

> `.vscode/settings.json`用来自定义文件夹结构.
